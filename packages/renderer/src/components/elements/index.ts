import { Row } from './Row';
import { Text } from './Text';
import { Input } from './Input';
import { Fragment } from './Fragment';

export const elements = {
  fragment: Fragment,
  input: Input,
  row: Row,
  text: Text,
};
