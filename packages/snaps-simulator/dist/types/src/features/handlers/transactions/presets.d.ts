import type { TransactionFormData } from './utils';
export declare type TransactionPreset = {
    name: string;
    transaction: TransactionFormData;
};
export declare const TRANSACTION_PRESETS: TransactionPreset[];
