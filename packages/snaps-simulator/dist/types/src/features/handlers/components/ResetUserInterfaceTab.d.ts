import type { FunctionComponent } from 'react';
/**
 * Resets the tab to the first tab when the user interface is closed.
 *
 * @returns `null`.
 */
export declare const ResetUserInterfaceTab: FunctionComponent;
