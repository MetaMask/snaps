{
  "name": "siwe-snap-example",
  "version": "0.33.1-flask.1",
  "private": true,
  "description": "A snap with a companion Dapp that uses SIWE to retrieve an API key",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MetaMask/snaps-monorepo.git"
  },
  "license": "(MIT-0 OR Apache-2.0)",
  "author": "Ziad Saab <ziad.saab@gmail.com>",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach --parallel --topological --verbose run build",
    "start": "yarn workspaces foreach --parallel --interlaced --verbose run start",
    "test": "echo \"TODO\""
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "packageManager": "yarn@3.2.1"
}
